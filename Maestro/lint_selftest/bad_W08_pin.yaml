appId: ${APP_ID}
---
# W08: PIN 숫자 탭 앞에 **화면 도달 판정이 없다**
#   화면이 아직 안 떴는데 누르면 같은 글자를 가진 다른 요소를 집는다.
#   (탭 사이 대기는 불필요 — 키패드는 화면 진입 시에만 재배열된다. 2026-09-01 실측)
- tapOn: "확인"
- tapOn: "1"
- tapOn: "2"
- tapOn: "1"
- tapOn: "2"
