# lint_flows 결과 — 2026-08-31 17:06

- 대상: `.` / 44개 파일
- **ERROR 197** / **WARN 198**

> 실행으로는 원리적으로 안 잡히는 부류만 검사한다. 좌표·타이밍·앱 결함은 실행만이 잡는다.

## [ERROR] E01_PKG_ID — 148건

| 파일 | 줄 | 내용 |
|---|---|---|
| 02_01_Overseas_Send now.yaml | 80 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 02_01_Overseas_Send now.yaml | 83 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 02_02_Overseas_Schedule.yaml | 65 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 02_02_Overseas_Schedule.yaml | 68 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_01_Change_SimplePassword.yaml | 42 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_01_Change_SimplePassword.yaml | 45 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_06_Settings_Change languages.yaml | 41 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_06_Settings_Change languages.yaml | 44 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 83 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 86 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 101 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 108 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 111 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 140 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 143 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 150 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 158 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 162 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 164 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 173 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 179 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 182 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 215 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 219 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 221 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 230 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 236 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 239 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 259 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 262 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 269 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 288 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 291 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 298 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 311 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 314 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 334 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 337 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 364 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 368 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 370 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 388 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 392 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 394 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 413 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 416 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 423 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 435 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 460 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 463 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 472 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 480 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 484 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 486 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 531 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 552 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 555 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 561 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 572 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 575 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 581 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 592 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 595 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 601 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 611 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 624 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 631 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 635 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 637 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 646 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 649 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 658 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 669 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 676 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 680 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 682 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 691 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 694 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 703 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 713 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 732 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 736 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 738 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 748 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 751 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 757 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 768 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 781 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 785 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 794 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 797 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 799 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 802 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 811 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 824 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 837 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 850 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 863 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 04_Menu_History.yaml | 877 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 07_TodaysRate.yaml | 55 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 07_TodaysRate.yaml | 58 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 08_MyQR.yaml | 75 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 08_MyQR.yaml | 78 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 08_MyQR.yaml | 253 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 08_MyQR.yaml | 352 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 15_Loan.yaml | 61 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 15_Loan.yaml | 65 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 15_Loan.yaml | 71 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 17_WalletStatement.yaml | 73 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 17_WalletStatement.yaml | 76 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 17_WalletStatement.yaml | 82 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 17_WalletStatement.yaml | 88 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 17_WalletStatement.yaml | 90 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 17_WalletStatement.yaml | 96 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 17_WalletStatement.yaml | 99 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 17_WalletStatement.yaml | 101 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 70 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 73 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 98 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 101 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 103 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 144 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 147 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 149 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 157 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 159 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 161 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 163 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 165 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 203 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 206 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 208 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 216 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 219 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 222 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 260 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 263 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 265 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 273 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 276 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 19_IssueCertificate.yaml | 279 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 21_Card.yaml | 53 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 21_Card.yaml | 56 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 21_Card.yaml | 145 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 23_MyWallet.yaml | 86 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 23_MyWallet.yaml | 89 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 24_BottomNav.yaml | 48 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |
| 24_BottomNav.yaml | 51 | id에 패키지명이 박혀 있다 → stag 빌드에서 절대 매칭되지 않고, 조건부 블록이면 에러 없이 SKIP된다. 짧은 id만 쓸 것 |

## [ERROR] E04_ENV_UNDEFINED — 5건

| 파일 | 줄 | 내용 |
|---|---|---|
| k_reg_setup.yaml | 53 | ${KB_ID} 가 env\*.env 에도, 어떤 flow의 env: 블록에도 없다 → 치환되지 않은 리터럴로 남아 조용히 SKIP된다 |
| k_reg_setup.yaml | 91 | ${KB_PHONE} 가 env\*.env 에도, 어떤 flow의 env: 블록에도 없다 → 치환되지 않은 리터럴로 남아 조용히 SKIP된다 |
| k_reg_setup.yaml | 99 | ${KB_NAT} 가 env\*.env 에도, 어떤 flow의 env: 블록에도 없다 → 치환되지 않은 리터럴로 남아 조용히 SKIP된다 |
| k_reg_setup.yaml | 102 | ${KB_NAT} 가 env\*.env 에도, 어떤 flow의 env: 블록에도 없다 → 치환되지 않은 리터럴로 남아 조용히 SKIP된다 |
| k_step_pick.yaml | 5 | ${KB_IDTYPE} 가 env\*.env 에도, 어떤 flow의 env: 블록에도 없다 → 치환되지 않은 리터럴로 남아 조용히 SKIP된다 |

## [ERROR] E05_WRONG_LABEL — 62건

| 파일 | 줄 | 내용 |
|---|---|---|
| _stag_login.yaml | 112 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 01_01_Login_First.yaml | 82 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 02_01_Overseas_Send now.yaml | 45 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 02_01_Overseas_Send now.yaml | 209 | 실측 문구와 다르다: "TESTUPDATE GME" → "GME TEST"  (실재하지 않는 수취인. 단 02_01의 when: visible: 방어 블록은 의도된 것일 수 있다) |
| 02_01_Overseas_Send now.yaml | 221 | 실측 문구와 다르다: "TESTUPDATE GME" → "GME TEST"  (실재하지 않는 수취인. 단 02_01의 when: visible: 방어 블록은 의도된 것일 수 있다) |
| 02_01_Overseas_Send now.yaml | 233 | 실측 문구와 다르다: "TESTUPDATE GME" → "GME TEST"  (실재하지 않는 수취인. 단 02_01의 when: visible: 방어 블록은 의도된 것일 수 있다) |
| 02_01_Overseas_Send now.yaml | 537 | 실측 문구와 다르다: "간편 비밀번호" → "간편 비밀번호를 입력하세요"  (02_01 [02]가 PIN을 입력한 적 없이 통과한 원인) |
| 02_01_Overseas_Send now.yaml | 548 | 실측 문구와 다르다: "간편 비밀번호" → "간편 비밀번호를 입력하세요"  (02_01 [02]가 PIN을 입력한 적 없이 통과한 원인) |
| 02_02_Overseas_Schedule.yaml | 48 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 02_02_Overseas_Schedule.yaml | 152 | 실측 문구와 다를 수 있다: "예약" → "예약하기"  (구 UI **홈 그리드** 실측 라벨은 예약하기다. 화면 내부의 예약 버튼/탭은 그대로일 수 있으니 문맥 확인 필요) |
| 02_02_Overseas_Schedule.yaml | 156 | 실측 문구와 다르다: "TESTUPDATE GME" → "GME TEST"  (실재하지 않는 수취인. 단 02_01의 when: visible: 방어 블록은 의도된 것일 수 있다) |
| 02_02_Overseas_Schedule.yaml | 212 | 실측 문구와 다르다: "TESTUPDATE GME" → "GME TEST"  (실재하지 않는 수취인. 단 02_01의 when: visible: 방어 블록은 의도된 것일 수 있다) |
| 02_02_Overseas_Schedule.yaml | 214 | 실측 문구와 다르다: "TESTUPDATE GME" → "GME TEST"  (실재하지 않는 수취인. 단 02_01의 when: visible: 방어 블록은 의도된 것일 수 있다) |
| 02_02_Overseas_Schedule.yaml | 223 | 실측 문구와 다르다: "TESTUPDATE GME" → "GME TEST"  (실재하지 않는 수취인. 단 02_01의 when: visible: 방어 블록은 의도된 것일 수 있다) |
| 02_02_Overseas_Schedule.yaml | 259 | 실측 문구와 다를 수 있다: "예약" → "예약하기"  (구 UI **홈 그리드** 실측 라벨은 예약하기다. 화면 내부의 예약 버튼/탭은 그대로일 수 있으니 문맥 확인 필요) |
| 02_02_Overseas_Schedule.yaml | 398 | 실측 문구와 다를 수 있다: "예약" → "예약하기"  (구 UI **홈 그리드** 실측 라벨은 예약하기다. 화면 내부의 예약 버튼/탭은 그대로일 수 있으니 문맥 확인 필요) |
| 02_02_Overseas_Schedule.yaml | 435 | 실측 문구와 다를 수 있다: "예약" → "예약하기"  (구 UI **홈 그리드** 실측 라벨은 예약하기다. 화면 내부의 예약 버튼/탭은 그대로일 수 있으니 문맥 확인 필요) |
| 02_02_Overseas_Schedule.yaml | 548 | 실측 문구와 다를 수 있다: "예약" → "예약하기"  (구 UI **홈 그리드** 실측 라벨은 예약하기다. 화면 내부의 예약 버튼/탭은 그대로일 수 있으니 문맥 확인 필요) |
| 02_02_Overseas_Schedule.yaml | 675 | 실측 문구와 다를 수 있다: "예약" → "예약하기"  (구 UI **홈 그리드** 실측 라벨은 예약하기다. 화면 내부의 예약 버튼/탭은 그대로일 수 있으니 문맥 확인 필요) |
| 02_02_Overseas_Schedule.yaml | 679 | 실측 문구와 다르다: "TESTUPDATE GME" → "GME TEST"  (실재하지 않는 수취인. 단 02_01의 when: visible: 방어 블록은 의도된 것일 수 있다) |
| 02_02_Overseas_Schedule.yaml | 681 | 실측 문구와 다르다: "TESTUPDATE GME" → "GME TEST"  (실재하지 않는 수취인. 단 02_01의 when: visible: 방어 블록은 의도된 것일 수 있다) |
| 02_02_Overseas_Schedule.yaml | 879 | 실측 문구와 다를 수 있다: "예약" → "예약하기"  (구 UI **홈 그리드** 실측 라벨은 예약하기다. 화면 내부의 예약 버튼/탭은 그대로일 수 있으니 문맥 확인 필요) |
| 02_02_Overseas_Schedule.yaml | 884 | 실측 문구와 다르다: "TESTUPDATE GME" → "GME TEST"  (실재하지 않는 수취인. 단 02_01의 when: visible: 방어 블록은 의도된 것일 수 있다) |
| 03_Domestic.yaml | 61 | 실측 문구와 다르다: "국내송금" → "국내 송금"  (구 UI 홈 그리드 실측 라벨(공백). 텍스트 전체매칭이라 조용히 SKIP된다) |
| 03_Domestic.yaml | 116 | 실측 문구와 다르다: "국내송금" → "국내 송금"  (구 UI 홈 그리드 실측 라벨(공백). 텍스트 전체매칭이라 조용히 SKIP된다) |
| 03_Domestic.yaml | 184 | 실측 문구와 다르다: "국내송금" → "국내 송금"  (구 UI 홈 그리드 실측 라벨(공백). 텍스트 전체매칭이라 조용히 SKIP된다) |
| 03_Domestic.yaml | 237 | 실측 문구와 다르다: "국내송금" → "국내 송금"  (구 UI 홈 그리드 실측 라벨(공백). 텍스트 전체매칭이라 조용히 SKIP된다) |
| 03_Domestic.yaml | 269 | 실측 문구와 다르다: "국내송금" → "국내 송금"  (구 UI 홈 그리드 실측 라벨(공백). 텍스트 전체매칭이라 조용히 SKIP된다) |
| 03_Domestic.yaml | 301 | 실측 문구와 다르다: "국내송금" → "국내 송금"  (구 UI 홈 그리드 실측 라벨(공백). 텍스트 전체매칭이라 조용히 SKIP된다) |
| 04_01_Change_SimplePassword.yaml | 31 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 04_02_Change_LoginPassword.yaml | 46 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 04_06_Settings_Change languages.yaml | 30 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 04_Menu_History.yaml | 35 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 04_Menu_remaining.yaml | 42 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 04_Menu_remaining.yaml | 398 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 06_Registration_All.yaml | 968 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 06_Registration_Foreigner.yaml | 436 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 06_Registration_Korean.yaml | 559 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 08_MyQR.yaml | 64 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 08_MyQR.yaml | 197 | 실측 문구와 다르다: "케이뱅크" → "신한은행"  (연동 계좌 목록에서 사라졌다(2026-08-28)) |
| 14_Deposit.yaml | 107 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 14_Deposit.yaml | 316 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 14_Deposit.yaml | 366 | 실측 문구와 다르다: "GMEPay 가상 계좌" → "GME Pay 가상 계좌"  (실제 라벨에 공백. assertNotVisible로 쓰여 아무것도 검증하지 못했다) |
| 14_Deposit.yaml | 450 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 14_Deposit.yaml | 492 | 실측 문구와 다르다: "GMEPay 가상 계좌" → "GME Pay 가상 계좌"  (실제 라벨에 공백. assertNotVisible로 쓰여 아무것도 검증하지 못했다) |
| 14_Deposit.yaml | 536 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 14_Deposit.yaml | 577 | 실측 문구와 다르다: "GMEPay 가상 계좌" → "GME Pay 가상 계좌"  (실제 라벨에 공백. assertNotVisible로 쓰여 아무것도 검증하지 못했다) |
| 14_Deposit.yaml | 626 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 14_Deposit.yaml | 667 | 실측 문구와 다르다: "GMEPay 가상 계좌" → "GME Pay 가상 계좌"  (실제 라벨에 공백. assertNotVisible로 쓰여 아무것도 검증하지 못했다) |
| 14_Deposit.yaml | 756 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 14_Deposit.yaml | 1169 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 14_Deposit.yaml | 1236 | 실측 문구와 다르다: "케이뱅크" → "신한은행"  (연동 계좌 목록에서 사라졌다(2026-08-28)) |
| 14_Deposit.yaml | 1520 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 14_Deposit.yaml | 1664 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |
| 19_IssueCertificate.yaml | 41 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 20_Booking.yaml | 56 | 실측 문구와 다를 수 있다: "예약" → "예약하기"  (구 UI **홈 그리드** 실측 라벨은 예약하기다. 화면 내부의 예약 버튼/탭은 그대로일 수 있으니 문맥 확인 필요) |
| 20_Booking.yaml | 89 | 실측 문구와 다를 수 있다: "예약" → "예약하기"  (구 UI **홈 그리드** 실측 라벨은 예약하기다. 화면 내부의 예약 버튼/탭은 그대로일 수 있으니 문맥 확인 필요) |
| 21_Card.yaml | 45 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 23_MyWallet.yaml | 75 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 23_MyWallet.yaml | 223 | 실측 문구와 다르다: "국내송금" → "국내 송금"  (구 UI 홈 그리드 실측 라벨(공백). 텍스트 전체매칭이라 조용히 SKIP된다) |
| 24_BottomNav.yaml | 37 | 실측 문구와 다르다: "로그인 세션이 만료" → "로그인 세션이 만료 + 개행 대응 패턴"  (문구에 개행이 섞여 있어 .* 로는 부족하다. 단일인용으로 개행 포함 패턴을 쓸 것) |
| 25_Profile.yaml | 342 | 실측 문구와 다르다: "간편비밀번호" → "앱에서 사용할 간편 비밀번호를 생성해 주세요"  (06_Registration 결함 #1. 조건 블록이 SKIP되어 PIN 화면에 갇혔다) |

## [ERROR] E07_LAUNCH_APPID — 1건

| 파일 | 줄 | 내용 |
|---|---|---|
| 01_Login_screen.yaml | 178 | 플로우 도중 launchApp이 특정 빌드를 하드코딩한다 → -AppId로 stag를 지정해도 이 줄에서 다른 빌드가 실행된다. ${APP_ID} 를 쓸 것 |

## [WARN] W01_GENERIC_LABEL — 30건

| 파일 | 줄 | 내용 |
|---|---|---|
| 02_01_Overseas_Send now.yaml | 70 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 03_Domestic.yaml | 52 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 04_Menu_History.yaml | 54 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 04_Menu_remaining.yaml | 222 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 04_Menu_remaining.yaml | 235 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 08_MyQR.yaml | 92 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 08_MyQR.yaml | 260 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 08_MyQR.yaml | 359 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 10_DomesticTopup.yaml | 31 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 13_ReceiveOverseas.yaml | 37 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 31 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 141 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 172 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 350 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 484 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 570 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 660 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 790 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 1203 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 1554 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 14_Deposit.yaml | 1698 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 15_Loan.yaml | 31 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 16_ATMWithdraw.yaml | 50 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 17_WalletStatement.yaml | 31 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 18_Telecom.yaml | 53 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 19_IssueCertificate.yaml | 60 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 20_Booking.yaml | 37 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 22_Event.yaml | 42 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 25_Profile.yaml | 51 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |
| 25_Profile.yaml | 376 | 범용 "닫기" 탭이다. 엉뚱한 X를 눌러 앱이 런처로 빠져나간 사고 이력이 있다 → id로 특정할 것(인앱 배너는 btnTwo\|btnClose) |

## [WARN] W02_NOTVISIBLE_UNPAIRED — 1건

| 파일 | 줄 | 내용 |
|---|---|---|
| 14_Deposit.yaml | 1428 | "909228615390" 가 어떤 파일에서도 긍정 검증(assertVisible 등)된 적이 없다 → 오타여도 통과한다. 같은 화면에서 실제 라벨을 한 번은 assertVisible로 확인할 것 |

## [WARN] W06_DUP_FIXTURE — 3건

| 파일 | 줄 | 내용 |
|---|---|---|
| 02_01_Overseas_Send now.yaml | 287 | 신원 픽스처가 271행과 중복된다. 서버가 소모하는 값이면 두 번째가 반드시 실패한다(여권/ARC 동일번호·S1/S2 동일 이메일이 실제 사고였다) |
| 02_01_Overseas_Send now.yaml | 439 | 신원 픽스처가 271행과 중복된다. 서버가 소모하는 값이면 두 번째가 반드시 실패한다(여권/ARC 동일번호·S1/S2 동일 이메일이 실제 사고였다) |
| 06_Registration_All.yaml | 827 | 신원 픽스처가 403행과 중복된다. 서버가 소모하는 값이면 두 번째가 반드시 실패한다(여권/ARC 동일번호·S1/S2 동일 이메일이 실제 사고였다) |

## [WARN] W07_RESULT_NO_WAIT — 6건

| 파일 | 줄 | 내용 |
|---|---|---|
| _stag_login.yaml | 80 | 결과 문구("오류") 갈래인데 앞 20줄에 extendedWaitUntil이 없다 → 아무것도 안 떠도 EXIT=0이 된다. 결과 도달을 먼저 강제할 것 |
| 01_01_Login_First.yaml | 50 | 결과 문구("오류") 갈래인데 앞 20줄에 extendedWaitUntil이 없다 → 아무것도 안 떠도 EXIT=0이 된다. 결과 도달을 먼저 강제할 것 |
| 14_Deposit.yaml | 83 | 결과 문구("오류") 갈래인데 앞 20줄에 extendedWaitUntil이 없다 → 아무것도 안 떠도 EXIT=0이 된다. 결과 도달을 먼저 강제할 것 |
| 14_Deposit.yaml | 426 | 결과 문구("오류") 갈래인데 앞 20줄에 extendedWaitUntil이 없다 → 아무것도 안 떠도 EXIT=0이 된다. 결과 도달을 먼저 강제할 것 |
| 14_Deposit.yaml | 1145 | 결과 문구("오류") 갈래인데 앞 20줄에 extendedWaitUntil이 없다 → 아무것도 안 떠도 EXIT=0이 된다. 결과 도달을 먼저 강제할 것 |
| 14_Deposit.yaml | 1640 | 결과 문구("오류") 갈래인데 앞 20줄에 extendedWaitUntil이 없다 → 아무것도 안 떠도 EXIT=0이 된다. 결과 도달을 먼저 강제할 것 |

## [WARN] W08_PIN_NO_WAIT — 95건

| 파일 | 줄 | 내용 |
|---|---|---|
| _stag_login.yaml | 40 | 숫자 탭 4개가 대기 없이 연속된다(40~43행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| _stag_login.yaml | 114 | 숫자 탭 4개가 대기 없이 연속된다(114~117행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| _stag_login.yaml | 119 | 숫자 탭 4개가 대기 없이 연속된다(119~122행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 01_01_Login_First.yaml | 84 | 숫자 탭 4개가 대기 없이 연속된다(84~87행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 01_01_Login_First.yaml | 89 | 숫자 탭 4개가 대기 없이 연속된다(89~92행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 01_02_Login_Wrong simple password.yaml | 24 | 숫자 탭 20개가 대기 없이 연속된다(24~45행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 01_02_Login_Wrong simple password.yaml | 95 | 숫자 탭 4개가 대기 없이 연속된다(95~98행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 01_02_Login_Wrong simple password.yaml | 100 | 숫자 탭 4개가 대기 없이 연속된다(100~103행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 02_01_Overseas_Send now.yaml | 539 | 숫자 탭 4개가 대기 없이 연속된다(539~544행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 02_01_Overseas_Send now.yaml | 550 | 숫자 탭 4개가 대기 없이 연속된다(550~555행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 02_02_Overseas_Schedule.yaml | 137 | 숫자 탭 4개가 대기 없이 연속된다(137~140행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 02_02_Overseas_Schedule.yaml | 584 | 숫자 탭 4개가 대기 없이 연속된다(584~588행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 02_02_Overseas_Schedule.yaml | 622 | 숫자 탭 4개가 대기 없이 연속된다(622~626행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 02_02_Overseas_Schedule.yaml | 744 | 숫자 탭 4개가 대기 없이 연속된다(744~748행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 03_Domestic.yaml | 37 | 숫자 탭 4개가 대기 없이 연속된다(37~40행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 03_Domestic.yaml | 78 | 숫자 탭 8개가 대기 없이 연속된다(78~86행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 03_Domestic.yaml | 98 | 숫자 탭 4개가 대기 없이 연속된다(98~101행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 03_Domestic.yaml | 140 | 숫자 탭 4개가 대기 없이 연속된다(140~144행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 03_Domestic.yaml | 156 | 숫자 탭 4개가 대기 없이 연속된다(156~159행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 03_Domestic.yaml | 218 | 숫자 탭 4개가 대기 없이 연속된다(218~221행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 03_Domestic.yaml | 250 | 숫자 탭 4개가 대기 없이 연속된다(250~253행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_01_Change_SimplePassword.yaml | 66 | 숫자 탭 4개가 대기 없이 연속된다(66~69행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_01_Change_SimplePassword.yaml | 96 | 숫자 탭 4개가 대기 없이 연속된다(96~99행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_01_Change_SimplePassword.yaml | 104 | 숫자 탭 4개가 대기 없이 연속된다(104~107행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_01_Change_SimplePassword.yaml | 110 | 숫자 탭 4개가 대기 없이 연속된다(110~113행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_01_Change_SimplePassword.yaml | 134 | 숫자 탭 2개가 대기 없이 연속된다(134~135행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_01_Change_SimplePassword.yaml | 189 | 숫자 탭 4개가 대기 없이 연속된다(189~192행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_01_Change_SimplePassword.yaml | 212 | 숫자 탭 4개가 대기 없이 연속된다(212~215행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_02_Change_LoginPassword.yaml | 34 | 숫자 탭 4개가 대기 없이 연속된다(34~37행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_Menu_History.yaml | 21 | 숫자 탭 4개가 대기 없이 연속된다(21~24행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_Menu_remaining.yaml | 30 | 숫자 탭 4개가 대기 없이 연속된다(30~33행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 04_Menu_remaining.yaml | 390 | 숫자 탭 4개가 대기 없이 연속된다(390~393행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_All.yaml | 477 | 숫자 탭 4개가 대기 없이 연속된다(477~480행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_All.yaml | 485 | 숫자 탭 4개가 대기 없이 연속된다(485~488행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_All.yaml | 869 | 숫자 탭 4개가 대기 없이 연속된다(869~872행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_All.yaml | 877 | 숫자 탭 4개가 대기 없이 연속된다(877~880행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_All.yaml | 971 | 숫자 탭 4개가 대기 없이 연속된다(971~974행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_All.yaml | 977 | 숫자 탭 4개가 대기 없이 연속된다(977~980행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_Foreigner.yaml | 337 | 숫자 탭 4개가 대기 없이 연속된다(337~340행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_Foreigner.yaml | 345 | 숫자 탭 4개가 대기 없이 연속된다(345~348행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_Foreigner.yaml | 439 | 숫자 탭 4개가 대기 없이 연속된다(439~442행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_Foreigner.yaml | 445 | 숫자 탭 4개가 대기 없이 연속된다(445~448행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_Korean.yaml | 460 | 숫자 탭 4개가 대기 없이 연속된다(460~463행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_Korean.yaml | 468 | 숫자 탭 4개가 대기 없이 연속된다(468~471행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_Korean.yaml | 562 | 숫자 탭 4개가 대기 없이 연속된다(562~565행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 06_Registration_Korean.yaml | 568 | 숫자 탭 4개가 대기 없이 연속된다(568~571행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 08_MyQR.yaml | 284 | 숫자 탭 4개가 대기 없이 연속된다(284~287행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 09_Home.yaml | 21 | 숫자 탭 4개가 대기 없이 연속된다(21~24행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 09_Home.yaml | 142 | 숫자 탭 4개가 대기 없이 연속된다(142~145행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 09_Home.yaml | 214 | 숫자 탭 4개가 대기 없이 연속된다(214~217행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 09_Home.yaml | 237 | 숫자 탭 4개가 대기 없이 연속된다(237~240행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 10_DomesticTopup.yaml | 15 | 숫자 탭 4개가 대기 없이 연속된다(15~18행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 10_DomesticTopup.yaml | 152 | 숫자 탭 4개가 대기 없이 연속된다(152~155행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 11_IntlTopup.yaml | 35 | 숫자 탭 4개가 대기 없이 연속된다(35~38행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 12_BillPayment.yaml | 36 | 숫자 탭 4개가 대기 없이 연속된다(36~39행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 13_ReceiveOverseas.yaml | 21 | 숫자 탭 4개가 대기 없이 연속된다(21~24행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 15 | 숫자 탭 4개가 대기 없이 연속된다(15~18행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 109 | 숫자 탭 4개가 대기 없이 연속된다(109~112행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 114 | 숫자 탭 4개가 대기 없이 연속된다(114~117행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 318 | 숫자 탭 4개가 대기 없이 연속된다(318~321행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 323 | 숫자 탭 4개가 대기 없이 연속된다(323~326행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 452 | 숫자 탭 4개가 대기 없이 연속된다(452~455행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 457 | 숫자 탭 4개가 대기 없이 연속된다(457~460행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 538 | 숫자 탭 4개가 대기 없이 연속된다(538~541행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 543 | 숫자 탭 4개가 대기 없이 연속된다(543~546행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 628 | 숫자 탭 4개가 대기 없이 연속된다(628~631행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 633 | 숫자 탭 4개가 대기 없이 연속된다(633~636행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 758 | 숫자 탭 4개가 대기 없이 연속된다(758~761행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 763 | 숫자 탭 4개가 대기 없이 연속된다(763~766행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 905 | 숫자 탭 4개가 대기 없이 연속된다(905~908행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 920 | 숫자 탭 4개가 대기 없이 연속된다(920~923행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 981 | 숫자 탭 4개가 대기 없이 연속된다(981~984행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 996 | 숫자 탭 4개가 대기 없이 연속된다(996~999행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 1171 | 숫자 탭 4개가 대기 없이 연속된다(1171~1174행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 1176 | 숫자 탭 4개가 대기 없이 연속된다(1176~1179행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 1522 | 숫자 탭 4개가 대기 없이 연속된다(1522~1525행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 1527 | 숫자 탭 4개가 대기 없이 연속된다(1527~1530행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 1573 | 숫자 탭 4개가 대기 없이 연속된다(1573~1576행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 1666 | 숫자 탭 4개가 대기 없이 연속된다(1666~1669행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 1671 | 숫자 탭 4개가 대기 없이 연속된다(1671~1674행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 15_Loan.yaml | 15 | 숫자 탭 4개가 대기 없이 연속된다(15~18행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 16_ATMWithdraw.yaml | 34 | 숫자 탭 4개가 대기 없이 연속된다(34~37행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 16_ATMWithdraw.yaml | 134 | 숫자 탭 4개가 대기 없이 연속된다(134~137행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 16_ATMWithdraw.yaml | 150 | 숫자 탭 4개가 대기 없이 연속된다(150~154행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 16_ATMWithdraw.yaml | 167 | 숫자 탭 4개가 대기 없이 연속된다(167~170행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 17_WalletStatement.yaml | 15 | 숫자 탭 4개가 대기 없이 연속된다(15~18행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 18_Telecom.yaml | 37 | 숫자 탭 4개가 대기 없이 연속된다(37~40행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 20_Booking.yaml | 21 | 숫자 탭 4개가 대기 없이 연속된다(21~24행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 20_Booking.yaml | 189 | 숫자 탭 4개가 대기 없이 연속된다(189~194행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 20_Booking.yaml | 248 | 숫자 탭 4개가 대기 없이 연속된다(248~251행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 22_Event.yaml | 26 | 숫자 탭 4개가 대기 없이 연속된다(26~29행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 25_Profile.yaml | 35 | 숫자 탭 4개가 대기 없이 연속된다(35~38행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 25_Profile.yaml | 344 | 숫자 탭 4개가 대기 없이 연속된다(344~347행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| 25_Profile.yaml | 349 | 숫자 탭 4개가 대기 없이 연속된다(349~352행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |
| charge.yaml | 19 | 숫자 탭 4개가 대기 없이 연속된다(19~22행). 잠금화면 PIN 키패드는 탭마다 재배치되므로 오입력이 누적되고 계정이 잠긴다 → 탭 사이 waitForAnimationToEnd. 보안 키패드(입력완료로 끝나는 것)면 화이트리스트에 넣을 것 |

## [WARN] W09_HEADER_APPID — 44건

| 파일 | 줄 | 내용 |
|---|---|---|
| _inbound_upload_docs.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| _server_override.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| _stag_login_probe.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| _stag_login.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 01_01_Login_First.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 01_02_Login_Wrong simple password.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 01_Login_screen.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 02_01_Overseas_Send now.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 02_02_Overseas_Schedule.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 03_Domestic.yaml | 2 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 04_01_Change_SimplePassword.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 04_02_Change_LoginPassword.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 04_06_Settings_Change languages.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 04_Menu_History.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 04_Menu_remaining.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 05_InitialScreen.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 06_Registration_All.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 06_Registration_Foreigner.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 06_Registration_Korean.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 07_TodaysRate.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 08_MyQR.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 09_Home.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 10_DomesticTopup.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 11_IntlTopup.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 12_BillPayment.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 13_ReceiveOverseas.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 14_Deposit.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 15_Loan.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 16_ATMWithdraw.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 17_WalletStatement.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 18_Telecom.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 19_IssueCertificate.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 20_Booking.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 21_Card.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 22_Event.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 23_MyWallet.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 24_BottomNav.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| 25_Profile.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| charge.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| k_reg_setup.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| k_step_done.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| k_step_passportno.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| k_step_pick.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
| k_step_shoot.yaml | 1 | 헤더 appId가 하드코딩돼 있어 -AppId 로 빌드를 바꿀 수 없다. 전용 빌드 파일이면 의도된 것일 수 있다 → 그렇다면 화이트리스트에 넣을 것 |
