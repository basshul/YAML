# PROGRESS.md

Maestro 테스트 플로우 작성·검증 **진행 현황 스냅샷** (최종 갱신 2026-09-10).

⚠️ 이 문서는 스냅샷이다. 케이스 단위의 최신 상태는 **ClickUp**(태스크 `86exntjkz`)이 정본이고,
파일별 상세 함정·발견사항은 **메모리 `project_*.md`**에 있다. 여기서는 "어디까지 했나"만 본다.
운영 지침은 [CLAUDE.md](CLAUDE.md) 참고.

## 두 개의 트랙

같은 앱에 홈이 두 벌 있어(구 UI `HomeActivityV2` / 개편 UI) 플로우도 두 벌이다.

| 트랙 | 위치 | 상태 |
|---|---|---|
| **개편 UI (V3)** | `Maestro\*.yaml` | **작성 완료**(2026-07-31 `12_BillPayment`가 마지막). yaml 34개 / 케이스 라벨 431개 / 미착수 placeholder 0건. **UI가 다시 바뀔 수 있어 후속 작업은 급하지 않다**(2026-09-02 사용자 판단) — 참고용 원본이며 편집하지 않는다 |
| **구 UI (V2)** | `Maestro\Old\*_old.yaml` | **전환은 전 파일 완료(2026-08-31).** 잔여는 전부 "검증"이다. 현재 시나리오 파일 **40개** + 헬퍼 13개(`_` 접두사) + EasyCare stag 2개. 마이그레이션 대상은 `23_MyWallet`(구 UI에 화면 없음)과 EasyCare를 뺀 나머지 |

개편 UI판을 그대로 `Old\`에 두면 진입 경로부터 깨진다. 전환 규칙은 CLAUDE.md의 "구 UI vs 개편 UI" 절.

## 기기 교체(2026-09-09) — 해상도가 바뀌면 무엇이 깨지는가

테스트 기기를 **1080x2220 → SM-S911N(1080x2340)** 로 바꾸자 **7개 파일**이 한꺼번에 실패했다.
증상은 제각각이었지만 원인은 하나다 — **요소가 화면 밖으로 밀리거나 키패드에 가려진다.**
전부 앱 결함이 아니라 **플로우가 화면 크기를 전제하고 있었던 것**이다.

| 실제로 나온 증상 | 진짜 원인 | 고친 방법 |
|---|---|---|
| 타겟이 아닌 **다른 송금인이 삭제**됨 (`13_06`) | `index:`는 행이 아니라 **그 순간 화면 맨 위**를 가리킨다 | 이름 앵커(`childOf: containsDescendants`) + `centerElement` + 자기교정 루프 |
| `Element not found` 인데 **화면에는 보인다** (`13_01` Address·City, `14_01` 금액 칩) | 키패드가 덮으면 **접근성 트리에서 사라진다** | 입력 직후 `hideKeyboard` |
| 앱이 홈으로 튀거나 폼을 벗어남 (`26_Menu`) | `hideKeyboard`는 **키보드가 없으면 뒤로가기**로 동작한다 | 입력 직후에만 두거나 `when:` 가드 |
| 고정 퍼센트 스와이프가 **엉뚱한 위치**에 멈춤 (`13_04`) | 같은 30%가 해상도마다 다른 행 수만큼 움직인다 | `scrollUntilVisible` + `centerElement: true` |
| 좌표 탭이 **아무 일도 안 했는데 통과** (`21_Card [19]`) | 좌표는 빗나가도 에러가 없다 → 거짓 통과 | 좌표를 버리고 `'라벨[\s\S]*'` 텍스트 탭으로 |
| 상단 필드를 못 찾음 (`04_Menu_History` 인바운드 상세) | 아래로 스크롤한 뒤엔 위쪽을 못 찾는다 | 단언을 **화면 순서대로** 재배치 |

### 다음 교체 때 반복하지 않으려고 한 일

1. **린터가 정적으로 잡는다** — `lint_flows.ps1`에 규칙 3개를 추가했다(자기검사 **22/22**).
   - `W11_COORD_NO_ASSERT` — 좌표 탭인데 같은 케이스에 결과 단언이 없다(= 빗나가도 통과)
   - `W12_HIDEKEYBOARD_AS_BACK` — 앞에 입력이 없는 `hideKeyboard`(= 뒤로가기로 동작)
   - `W13_INDEX_AFTER_SCROLL` — 스크롤/스와이프 직후의 `index:`(= 다른 행을 집는다)
   ⚠️ 창은 **빈 줄을 건너뛰며** 센다. 이 저장소는 함정 하나당 주석이 4~5줄이라
   "직전 N줄"로 잡으면 창이 통째로 주석에 먹힌다(초안이 정상 코드 9곳을 오탐으로 냈다).
2. **작성 규칙은 스킬에 있다** — `maestro-yaml` 의 "기기·해상도가 바뀌어도 버티게 쓰기".
   실행 전 점검은 `maestro-run` 의 "기기가 바뀌었을 때".
3. 잔존 결함 2건을 함께 고쳤다: `13_05 [13]`(고정 스와이프 + `index: 0` → 이름 앵커),
   `21_Card [19]`(좌표 탭 2곳 → 텍스트 탭). ⚠️ `21_Card [19]`는 **다음 실행에서 확인이 필요하다**
   (좌표를 안 쓰게 됐지만 `'해외 원화[\s\S]*'` 매칭 자체는 실기기로 아직 못 봤다).

## 구 UI 재작성 — 완료 (26)

`EXIT=0 2회`는 연속 2회 무결 실행으로 확정했다는 뜻이다.

| 파일 | 결과 | 비고 |
|---|---|---|
| `01_01_Login_First_old` | EXIT=0 / 5케이스 | 세션 만료 시 복구용으로도 쓴다 |
| `01_Login_screen_old` | EXIT=0 / 7케이스 (2026-08-26) | `clearState`+[06]이 **비밀번호를 실제 초기화**한다 → 아래 참고 |
| `04_01_Change_SimplePassword_old` | EXIT=0 / 8케이스 | PIN 변경 → 원복 왕복 |
| `04_02_Change_LoginPassword_old` | **[06]~[13] 전 구간 통과**(2026-09-01, stag) | `[13]` 원복 제출 성공 + 원래 비밀번호 재로그인 확인(`01_01` EXIT=0). 통짜 EXIT=0 기록만 없음(원복 후 재로그인 중 외부 중단). 홈 팝업 헬퍼 3곳 선제 추가 |
| `04_06_Settings_Change languages_old` | EXIT=0 / 24개 언어 순회 | 언어 변경 시 앱 재시작(세션은 유지) |
| `04_Menu_History_old` | **stag PASS / 375스텝 / 16:57**(2026-09-01) | `[40]`~`[48]` 포함 48케이스 완주. 총 9회 실행에 **결함 6건** 수정(한글 이름 `inputText` / 카테고리·차트 데이터 의존 / 서버조회 대기 / 청구서 진입 재시도 / 진입부 팝업 문구). 🐞청구서 1개월 필터 오류는 **현재 재현 불가**(데이터 0건) |
| `05_InitialScreen_old` | EXIT=0 3회 / 148스텝 / 8케이스 | `clearState` 5곳인데도 LIVETEST 유지(호스트 30건 전부 livetest). 실행 후 재로그인 필요 |
| `02_02_Overseas_Schedule_old` | EXIT=0 / 310스텝 / 6케이스 | 구 UI 전환 완료(2026-08-28). ⚠️ **`TESTUPDATE GME` 수취인이 실재하지 않아** `GME TEST`(스리랑카)로 교체 |
| `03_Domestic_old` | EXIT=0 / 144스텝 / 6케이스 | 잔액 충전 후 `[03]`~`[06]` BLOCKED 해제(2026-08-28). 홈 라벨은 **"국내 송금"**(공백) |
| `02_01_Overseas_Send now_old` | **PIN 직전까지 EXIT=0 / 133스텝**(2026-08-31, Live 빌드) | `[01]` 전 구간 + `[02]`의 수취인 추가→바텀시트 "예"→계산→2단계→PIN 화면 도달까지 검증. **PIN 입력 이후만 미검증**(실송금 자체는 별도 플로우로 운영 완주 확인). 아래 참고 |
| `07_TodaysRate_old` | EXIT=0 / 4케이스 | 진입 시 USD 계산오류 다이얼로그를 먼저 닫아야 함 |
| `08_MyQR_old` | EXIT=0 3회 / 239스텝 / 6케이스 | ⚠️ `[02]`의 **`케이뱅크`가 연동 목록에서 사라져** `신한은행`으로 교체(2026-08-28) |
| `09_Home_old` | EXIT=0 / 15케이스 | 외국인 분기 [01]만 별도 확인 |
| `10_DomesticTopup_old` | EXIT=0 2회 / 82스텝 | 공용 허브 경유 |
| `11_IntlTopup_old` | EXIT=0 2회 / 71스텝 | 간편비번 화면 직전 정지(실결제 방지) |
| `12_BillPayment_old` | EXIT=0 2회 / 69스텝 | 동일 |
| `13_ReceiveOverseas_old` | 실기기 100% PASS | 자가복구 로직 적용 |
| `15_Loan_old` | EXIT=0 / 1케이스 | ⚠️ seungsoo818이 **한국인**일 때만 통과 |
| `16_ATMWithdraw_old` | EXIT=0 2회 / 94스텝 / 4케이스 | 취소 재시도 루프 필수 |
| `17_WalletStatement_old` | EXIT=0 / 2케이스 | 잔액 로딩 최대 90초 |
| `18_Telecom_old` | **EXIT=0 / 82스텝**(2026-09-03) | `[01]`~`[05]`,`[08]` 커버. `[06][07]`만 **외부 인증**으로 BLOCKED |
| `19_IssueCertificate_old` | EXIT=0 2회 / 4케이스 | 인증수단 선택 단계까지 |
| `22_Event_old` | EXIT=0 2회 / 57스텝 | Compose UI라 id 없음 |
| `25_Profile_old` | EXIT=0 / 10케이스 | 외국인 분기는 `25_Profile_Foreign_old`로 분리 |
| `26_Menu_old` | EXIT=0 / 20케이스 | `04_Menu_remaining_old` 11건 흡수 후 그 파일은 삭제 |
| `14_Deposit_old` | **계정별 3파일 분할 후 전부 EXIT=0** | 35케이스. 원본은 참고용 → 아래 참고 |

### 대상 아님 / 별도 트랙

- `23_MyWallet_old` — **구 UI에 "My Wallet" 화면이 존재하지 않는다.** 파일은 N/A 문서로만 남김(실행 케이스 0).
  종전 4케이스는 `09_Home_old [09][10]`과 `14_Deposit_old`로 귀속.
- `27_EasyCare_i18n_*_stag_old` — appId가 `.stag`인 별도 검증 건(언어별 UI). livetest 회귀 대상 아님.

### `14_Deposit` 계정별 3분할 (2026-08-26 완료)

원본 1845줄/35케이스가 3계정을 오가느라 1회 실행이 너무 길어 **계정별로 분리**했다(사용자 지시).
**3개 파일 모두 첫 실행에서 EXIT=0 / FAILED 0건**을 받았다(2026-08-26).

| 파일 | 계정 | 케이스 | 스텝 | 로그인 | 결과 | 소요 |
|---|---|---|---|---|---|---|
| `14_01_Deposit_seungsoo818_old` | seungsoo818 (한국인·은행연결) | [01]~[05] [08] [25]~[28] [30]~[35] | 163 | 1회 | EXIT=0 | **10:28** |
| `14_02_Deposit_test123_old` | test123 (외국인·은행 미연결) | [06] [07] [09] [14]~[24] [29] | 215 | 1회 | EXIT=0 | **12:35** |
| `14_03_Deposit_test251024_old` | test251024 (외국인·GMEPay 신청완료) | [10]~[13] + seungsoo818 원복 | 90 | 2회 | EXIT=0 | **7:55** |

**이 상태로 `14_Deposit`은 완료 처리한다**(2026-08-26, 사용자 판단).
⚠️ 단 이 파일군은 **`EXIT=0` 1회로 닫았다** — 다른 파일에 적용한 "EXIT=0 2회" 관례와 다르다.
회귀 때 흔들리면 2회차를 먼저 돌려볼 것. 측정도 각 1회뿐이라 `[18]`/`[20]`의 외부 브라우저
(checkpay.co.kr) 구간 때문에 소요시간은 ±1분 정도 변동할 수 있다.

`14_02`가 12분 35초로 목표 10분을 넘지만 **재분할하지 않기로 했다**(사용자 판단). 이유:
- 실제로 시간에 걸리는 제약은 `[29]`가 만드는 편의점 입금 요청의 **10분 만료**뿐인데,
  `[29]`가 파일 맨 끝에 있어 연속 재실행 시 이미 만료된 상태다 — **긴 실행이 오히려 안전하다**.
- 쪼개면 로그인 고정비가 하나 더 붙어 총합이 12:35 → 약 14:30으로 **늘어난다**.

원본 `14_Deposit_old.yaml`은 참고용으로 남겨둔다. ⚠️ 케이스는 번호순이 아니라 **계정 묶음 순**이다.

**고정비가 크다**: 공통 진입 + 로그인 1회가 파일당 3~4분이다(잠금화면 → 로그아웃 → `loginpwidhidpass`
→ PIN 재등록). 90스텝짜리 `14_03`이 7분 55초인 이유가 이것(로그인 2회). 분할할수록 이 비용이 곱해지므로
"파일을 더 잘게" 가 항상 이득은 아니다. 실행 스텝 자체는 대략 **2.4초/스텝**.

### `01_Login_screen` 완료 (2026-08-26) — 결함 3건 발견

이 파일은 **개편 UI 기준으로 작성된 뒤 실기기 검증을 한 번도 거치지 않은** 상태였고, 결함 3건이 있었다.

| 증상 | 실제 원인 | 수정 |
|---|---|---|
| `[00]`에서 EXIT=1 | `LoginV2Activity`가 **FLAG_SECURE** | `takeScreenshot` → 화면 구성요소 `assertVisible` 5종 |
| `[05]` `Element not found: confirmBtn` | 좌표 `68%`가 날짜 휠의 **위쪽 버튼**이라 포커스가 안 잡히고, 이어지는 `pressKey: Enter`가 **바텀시트를 닫아** confirmBtn이 사라졌다. 게다가 날짜가 **틀린 값으로 조용히** 들어가고 있었다(1973-07-04 / 1988-08-26) | 좌표 폐기 → `numberpicker_input` **index 0/1/2**, 단계별 `confirmBtn` 가드, `assertVisible: "1988-08-18"` 값 검증 |
| `[05-7]`에서 EXIT=1 | 조회결과 팝업을 닫으면 **로그인 화면으로 복귀** → 또 FLAG_SECURE | assert로 대체. 같은 패턴이 예상되는 `[06-4]`도 선제 수정 |

⚠️ **`[06]`은 비밀번호를 실제로 초기화한다.** 2026-08-26 실행으로 `seungsoo818`의 비밀번호가 앱이 생성한 임의값으로 바뀌었고,
모든 yaml이 **정상 비밀번호를 하드코딩**하므로 **admin 재설정으로 되돌려야 한다**(사용자가 당일 완료).
`04_02_Change_LoginPassword_old`로는 원복 불가 — 그 파일은 **정상 ↔ 대체 비밀번호 왕복 전용**이다.
→ 회귀에 이 파일을 넣을 때는 **admin 재설정을 즉시 할 수 있는 상황에서만** 돌릴 것.

### `06_Registration` (2026-08-26) — 결함 **11건** 발견, 구간별 전 구간 통과

메모리엔 "실기기 실행 확인만 남음"으로 적혀 있었으나 **실제로는 결함이 11건 있었다.**
이 파일도 개편 UI 기준으로 작성된 뒤 구 UI 실기기 검증을 거친 적이 없었다.

| # | 증상 | 실제 원인 | 발견 |
|---|---|---|---|
| 1 | 조건 블록이 조용히 SKIP → PIN 화면에 갇힘 | `visible: "간편비밀번호"` — 텍스트는 **전체매칭**, 실제 문구는 "앱에서 사용할 간편 비밀번호를 생성해 주세요" | 정적 |
| 2 | 로그아웃 실패 | `${NAV_PROFILE}` → "로그아웃" = **개편 UI 경로**. 구 UI는 드로어(`iv_nav`)에만 있다 | 정적 |
| 3 | 셔터 탭이 빗나감 | 좌표 `50%,85%` — 실제 `captureBtnImageView` 중심은 **y≈91.6%**. 갤러리 버튼은 `75%`/`83%` 두 값이 섞여 있었다 | 실측 |
| 4 | 플로우 전체 사망 ×4회 | **캡처 불가 화면의 `takeScreenshot`** 44곳 | 실행 |
| 5 | `Element not found: 완료` | 갤러리 경로에 `완료` 탭이 **하나 더** 있었다(카메라 시퀀스를 복사) | 실행 |
| 6 | `Element not found: 나중에` | 지문인증 화면은 **생체인증 OFF 기기에선 안 뜬다**("항상 노출"이라는 주석이 틀렸다) | 실행 |
| 7 | `Screenshot returned null` | 비동기 전이가 끝나기 전에 스크린샷이 찍혔다(`[16a-0]`) | 실행 |
| 8 | `Duplicate id number.` | **여권과 ARC가 똑같이 `1234567890`** → 서로도 충돌하는 구조 | 서버 |
| 9 | `Customer with email ... already exist.` | **S1과 S2가 똑같이 `test@test.com`** → S1 성공 시 S2 필패 | 서버 |
| 10 | 이메일 `tapOn` 실패 | placeholder로 잡아 재진입 시 실패(필드에 값이 남음) → `emailInputField` id로 교체 | 실측 |
| 11 | `Element not found: btn_submit` | 보안 키패드의 **`입력완료`가 로그인을 자동 제출**한다(정상 파일 `01_01_Login_First_old`에도 이 탭이 없다). 덤으로 로그인 후 **보이스피싱 시트 처리 블록도 없었다** | 실행 |

**캡처 차단 범위(실측 확정)** — 카메라 프리뷰(`OCRImageCaptureActivity`) / 크롭(`NativeCropActivity`) /
**갤러리 피커** / 신분증 정보 화면(`DriverIdVerficationActivity` 계열) / 로그인 화면(`LoginV2Activity`).
캡처가 되는 곳은 **신분증 타입 선택 화면뿐**이다.
⚠️ 판정은 `adb exec-out screencap -p`의 **바이트 수**로 한다. `dumpsys window windows | grep -m1 "fl="`는
포커스가 아닌 첫 윈도우를 집을 수 있어 오판한다(이 세션에서 실제로 한 번 잘못 판정했다).

**⚠️ 이 파일은 멱등하지 않다.** 성공 1회당 사용자 ID·이메일·신분증 번호(여권/ARC)·휴대폰 번호가
서버에서 소모된다 → **재실행 전 `test007`/`test006`을 admin에서 지우거나 값을 모두 새로 바꿔야 한다.**
지우지 않으면 ID 중복 → 이메일 중복 → 신분증번호 중복 순으로 계속 막힌다.
현재 값: ID `test007`/`test006` · 이메일 `test007@test.com`/`test006@test.com` ·
여권 `9876543210` · ARC `9876543211`.

**검증 방식** — `test007` 가입이 완료된 뒤로는 통짜 재실행이 ID 중복으로 막히므로
**구간을 분리해 5회에 나눠** 검증했다(각 구간 EXIT=0, 총 ~560스텝).
전 구간이 통과했고 **`test007`·`test006` 두 계정 모두 admin에서 생성 확인됐다**(2026-08-27).
다만 **파일 전체를 한 번에 돌린 EXIT=0은 아직 없다** — 통짜 확인은 두 계정 삭제 후에 해야 한다.

**`06_Registration_Korean` / `_Foreigner`** — 2026-08-27에 `All` 기준으로 **코드 동기화 완료**.
주석을 제외한 **실행줄이 `All`과 완전히 일치**한다(diff 0):
`Korean` ≡ `All`의 S1 구간 + 복귀 꼬리 / `Foreigner` ≡ `All`의 S2 구간 + 복귀 꼬리.

이 대조 과정에서 **12번째 결함**이 나왔다 — **두 파일 모두 Server Override 스플래시 처리
블록이 통째로 빠져 있었다**(`extendedWaitUntil` + `runFlow: _server_override_old.yaml`).
livetest 빌드는 실행마다 이 스플래시가 뜨므로, 그대로 돌렸다면 스플래시가 화면을 덮은 채
`"신규 회원"` 탭이 실패해 **첫 화면에서 죽었을 것**이다. `All`과 동일하게 채웠다.
→ 교훈: "같은 내용을 분리만 한 파일"이라도 **분리 시점의 누락**이 있을 수 있다.
  정적 대조는 집합이 아니라 **순서까지 보는 diff**로 할 것(집합 비교로는 이 누락이 안 잡혔다).

⚠️ 두 파일은 여전히 **실행 검증은 못 했다** — `test007`/`test006`이 소모된 상태다.
코드가 `All`과 동일하고 `All`이 실기기로 전 구간 통과했으므로 동작은 같을 것으로 본다.

## 구 UI 재작성 — 잔여 — 전부 "전환"이 아니라 "검증"이 남은 것

| 파일 | 메모 |
|---|---|
| `01_02_Login_Wrong simple password_old` | ⚠️ **PIN 10회 오입력으로 계정을 잠그는** 시나리오 — 별도 확인 후 진행 |
| `06_Registration_Korean_old` / `_Foreigner_old` | `All` 기준 **코드 동기화 완료(실행줄 diff 0)**. `test007`/`test006` 소모로 **실행 검증만 미완** — 두 계정 삭제 시 실행 가능 |
| `20_Booking_old` | 실결제+자동취소 사이클. 날짜는 최대한 먼 미래로(취소 수수료) |
| `21_Card_old` | GME Shop "구매하기" 비활성화 이슈 미해결. ⚠️ 잔액부족 시 실계좌 자동충전. **2026-09-03 사용자 결정: 회귀 스위트 검증에서 확인되므로 단독 실행하지 않는다** → QA 체크리스트 32행도 그 결과로 채운다 |

### 로그인 계열 분리 (2026-09-01~02) — 회귀 스위트 작업에서 파생

`01_Login_screen_old`의 `[06]`이 **비밀번호를 실제로 초기화**해 스위트에 통째로 넣을 수 없었다.
그래서 파괴적 구간을 떼어냈다. ⚠️ **다른 세션이 진행 중이며, 검증 상태는 이 문서로 확정하지 않는다.**

| 파일 | 케이스 | 성격 |
|---|---|---|
| `01_03_Login_screen_safe_old` | 5 | 원본의 **비파괴 구간 `[01]`~`[05]`** — 회귀 스위트에는 이 파일을 넣는다 |
| `01_04_Login_Wrong_LoginPassword_old` | 4 | 로그인 비밀번호 5회 실패 동작 |
| `01_05_Login_Password_Reset_old` | 2 | 원본 `[06] [07]` — **admin에 붙을 수 있을 때만** 돌린다 |

원본 `01_Login_screen_old.yaml`(7케이스)은 그대로 남아 있다.

⚠️ 이 작업에서 나온 실측 — **Maestro 2.5.1은 플로우 헤더의 `env:` 기본값이 `--env`를 덮어쓴다**
(`01_04` 헤더, 2026-09-01. env 기본값이 있는 프로브는 `--env`를 줘도 FAILED, 없는 프로브는 COMPLETED).
헬퍼에 기본값을 박아두면 `run_test.ps1`이 넘긴 값이 **에러 없이 무시된다** — CLAUDE.md의
"반복되는 문자열은 `env:`로 변수화한다"를 따를 때 같이 알고 있어야 하는 함정이다.


### 로그인·회원가입 5개는 구/개편 UI 차이가 없다 (2026-08-25 검증)

yaml 정적 비교(차이는 i18n 변수·헬퍼 파일명뿐) + APK 매니페스트(`LoginV2Activity` 단일,
가입도 단일 세트) + 레이아웃 리소스(`activity_login_v2` 한 벌) 3중으로 확인했다.
**단 한 곳만 갈린다 — 가입 완료 후 도착하는 홈**: 구 UI 홈에는 인사말이 없어
`assertVisible "${TXT_GREETING}"` 판정이 깨진다 → `bottom_item_home` + UP 스크롤 후 `btnTransfer`로 교체함
(`.bak_20260825` 백업 있음). 그래서 이 5개는 **실기기 실행 확인만 남은 상태**다.

### `03_Domestic` (2026-08-27) — 화면 본문은 **손댈 것이 없었다**

사용자 가설("국내송금 화면은 구/개편 UI 동일")이 **맞았다.** 계좌번호 → 은행칩 → 금액 → 계속 →
확인 → 간편비밀번호 → 영수증까지 **한 줄도 고치지 않고** 실기기 통과했다
(`[01]` 2원 실송금 완주, `[02]` 영수증 다운로드 포함).

고친 것은 **전부 화면 바깥**이다.

| 수정 | 건수 | 증상 |
|---|---|---|
| 진입 블록 → 구 UI 표준 | 1 | `Server Override` 미처리 → **첫 스텝부터 사망**(스플래시에 멈춘 채 PIN 키패드 탭) |
| `text: "Home"` → `id: bottom_item_home` | 6 | 구 UI 홈엔 "Home"이 없어 `notVisible` 가드가 **항상 참** → PIN 블록 오작동 |
| `"국내송금"` → **`"국내 송금"`**(공백) | 6 | 구 UI 홈 그리드 실측 라벨. 텍스트 전체매칭이라 안 잡힌다 |
| 범용 `"닫기"` → `btnTwo` id | 1 | 런처 이탈 사고 이력 |

⚠️ 라벨 공백 건은 `14_Deposit`의 `"GMEPay 가상 계좌"` vs `"GME Pay 가상 계좌"`와 같은 유형이다.

**⛔ `[03]`~`[06]`은 GME Wallet 잔액 부족으로 막혔었다 → 2026-08-28 충전으로 해제, 6케이스 전부 PASS**
(아래는 당시 기록이다).  `Request balance is insufficient!`
실행 시작 138원 → `[01][02]`가 6원 사용 → `[03]`(타인 은행계좌, **수수료 발생**)에서 바닥.
종료 시 132원. 수수료·총액 표시와 검증은 정상 통과했으므로 **UI 문제가 아니다**.
→ 재개 조건이던 **지갑 충전은 완료됐다**(2026-08-28). 현재 이 파일은 EXIT=0 / 144스텝 / 6케이스다.

**미착수 항목은 없었다** — 주석 처리된 실행 스텝 0건. 헤더의 `[04] ... (TODO)`와
메모리의 "TODO 다수"는 둘 다 낡은 기재였다(실제 TODO 문자열 1개, 본문 코드는 완비).

⚠️ `[01]`은 설계상 **PIN을 1회 일부러 틀린다**(오류용 PIN → 정상 PIN). 반복 실행 시 계정 잠금에 유의.

### `02_01_Overseas_Send now` (2026-08-31) — **Live 빌드에서 실송금 확인 + 본 파일 반영**

`[02]`는 "전체매칭 가드가 죽어 실송금이 성립한 적 없다"는 상태로 오래 남아 있었다.
**운영 빌드(`com.gmeremit.online.gmeremittance_native`)에서 실제로 보내 앱 동작을 먼저 확정**한 뒤,
그 실측을 본 파일에 반영하고 다시 돌리는 순서로 진행했다.

**실송금 완주(운영)** — 총 송금액 12,017 KRW, 수수료 5,000원은 쿠폰 `Service Discount 100%`로 면제 →
**실제 차감 7,017원**(잔액 71,586 → 64,569, 정확히 일치). 결과 화면은
**"성공 / 거래가 정상적으로 완료되었습니다."** + `영수증 보기` / `다시 보내기`.

| 실측 | 내용 |
|---|---|
| 운영 빌드 구조 | **구 UI(`HomeActivityV2`) 동일** → `Old\` 플로우가 그대로 통한다. **Server Override 스플래시는 없다**(헬퍼가 조건부라 SKIP) |
| 진입 방해 | 실행 시 **얼굴 인증 / 문서 인증**(`FaceVerificationInitialScreenActivity` / `StartVerificationWebActivity`)이 홈 위에 올라올 수 있다 → `back` 이탈 필요 |
| ⛔ 2단계 함정 | 콘텐츠가 화면보다 길어 "계속"이 **화면 밖**인데 트리 bounds는 `[54,1920][1026,2060]`으로 남는다. 그 좌표에 그려진 건 **"2. 송금 사기 주의"(`txt_fraud_warning`) 링크** → 탭이 **COMPLETED로 찍히고도** 약관 브라우저가 열려 PIN에 도달하지 못한다. **`swipe: UP` 후** `agreeButton` |
| 잔액 검증 시점 | 종전 기재 "PIN 전까지 검증 안 함"은 **틀렸다.** **계산 단계에서 검증**되며 부족하면 "거래를 진행하기 위한 잔액이 부족합니다" 시트가 뜬다 |
| ⚠️⚠️ 자동충전 | 그 시트에는 **`자동이체 계좌 선택`(계좌 목록 + `btnVerifyBankAccount`)이 함께 뜬다.** 고르고 확인을 누르면 **실계좌 자동충전** → 자동으로 누르지 말 것 |
| 최소 송금액 | **환율 연동이라 매일 바뀐다**(스리랑카 2026-08-31 기준 12,017 KRW, `limitErrorMsg`로 통보) → 값을 본문에 박지 않는다 |

**본 파일 반영(179스텝)** — `env: SENDNOW_AMOUNT`(기본 14000) 추가 / 1·2단계 **도달 강제** /
잔액부족·`limitErrorMsg` **조기 실패**(없으면 `agreeButton` 대기가 타임아웃 나서 원인이 가려진다) /
2단계 `swipe: UP` + 약관 오탭 **자가복구** / 결과 성립 **단언** / 홈 복귀 `repeat while notVisible bottom_item_home`.
⚠️ `repeat`에 **`maxRuns`는 없다** — 이 저장소에서 실제로 도는 형태는 `while` + `commands`.

**검증 결과** — Live 빌드 **EXIT=0 / 133스텝**(PIN 직전까지). `[01]` 전 구간 +
`[02]`의 **수취인 신규 추가 → 저장 바텀시트 "예" → 금액 → 계산 → 2단계 → PIN 화면 도달**.
**남은 미검증은 PIN 입력 이후**(오입력 1회 분기 + 새 `repeat` 홈 복귀)뿐이다.

⚠️ 도중에 **세션 만료**로 한 번 중단됐다. 8/28에 고친 `[\s\S]*` 가드가 개행 섞인 2문장을 정확히 잡았고,
홈 대기보다 **앞**에 둔 덕에 원인이 로그 첫 줄에 드러났다 — 그 수정이 작동함이 확인됐다.
구 UI는 PIN으로 복구되지 않으므로 **ID/비밀번호 재로그인**이 필요하다.

⚠️ **임시본은 반드시 재추출한다.** 본 파일만 고치고 임시본을 그대로 돌려
**한 세대 전 버전을 검증하고 통과로 착각한 일이 실제로 있었다**(잔액부족 가드가 빠진 채 EXIT=0).

### 실결제 항목 진행 (2026-08-28) — 7.20.0-stag + LIVETEST

보류돼 있던 실결제 3건을 진행했다. **세 파일 모두 결제 자체는 완주했고, 실패는 전부 결제 이후
"홈 복귀" 구간**이었다 — 테스트 본질은 통과한 상태다.

| 파일 | 결제 | 홈 복귀 |
|---|---|---|
| `20_Booking` | **완주** — KTX 실결제 + **자동취소**까지(107스텝) | 수정 완료·미검증 |
| `11_IntlTopup` | **완주** — 스리랑카 Dialog 50 LKR 충전(84스텝) | 수정 완료·미검증 |
| `12_BillPayment` | **완주** — 네팔 NTC PREPAID 결제(77스텝) | 수정 완료·미검증 |
| `10_DomesticTopup` | **PASS (89스텝)** — 실자금 이동 없음 | 통과 |

**`11`·`12` 확장 방식** — 종전엔 간편비밀번호 화면에서 정지했다(실결제 방지 가드).
PIN 입력 + 결과 확인을 추가하되 판정을 2단으로 했다:
`extendedWaitUntil`에 **실패 문구까지** 넣어 응답 도달을 먼저 기다리고,
그다음 성공 문구만 `assertVisible` → **잔액 부족·서버 거부가 조용히 통과되지 않는다.**

⚠️ **`11`·`12`는 취소 절차가 없다.** `20_Booking`과 달리 되돌릴 수 없어 **실행할 때마다 금액이
누적**된다. 회귀 스위트 상시 포함 여부는 미결.
⚠️ 앱은 **PIN 입력 전까지 잔액을 검증하지 않는다** → 실행 전 잔액 확인 필수.

**성공 UI가 11과 12에서 다르다**: `11`=다이얼로그(`btn_diaog_ok`, 앱 오타 `diaog`) /
`12`=**바텀시트**(`btnOk`). `11` 기준으로 `12`를 고쳤다가 조건이 안 맞아 또 실패했고,
두 파일 모두 `btn_diaog_ok|btnOk`로 함께 받도록 정리했다.
⚠️ `12`의 `btn_save_biller`("청구 정보 저장")는 **누르면 안 된다**.

**`10_DomesticTopup`은 확장 대상이 아니었다**(2026-08-28 사용자 확인) —
**테스트에 쓰는 번호가 선불폰(PPS) 번호가 아니라서** 서버가 거부하는 것이고,
**그 거부 자체가 케이스 정의**다. 플로우는 이미 PIN까지 완주하며 실자금 이동이 없다.
→ `11`·`12`와 달리 **회귀에 반복해 넣어도 비용이 누적되지 않는다.**

⚠️ **7.20.0에서 문구·id가 조용히 바뀌는 사례가 반복됐다** — 오늘만 4건:
배너 `btnTwo`→`btnClose` / 홈 그리드 `예약`→`예약하기` /
성공 UI 다이얼로그(11) vs 바텀시트(12) / 충전 실패 문구
`인터넷 접속이 원활하지 않습니다`→**`실패 - (계좌번호)`**.
마지막 건은 **실패 응답이 정상적으로 왔는데도 3분 타임아웃으로 죽었다** —
빌드가 올라가면 셀렉터부터 의심할 것.

⚠️ **잔액 검증 시점이 화면마다 다르다**: `10`은 계좌 선택 화면에서 미리 막아
`confirm_btn`이 비활성(`low_balance`)이 되고 **PIN 화면까지 가지 않는다**(증상은
`input_dot_1 is visible` 실패라 셀렉터 문제로 오진하기 쉽다). 반면 `11`·`12`는
**PIN 입력 후 서버가 거부**한다. 실결제 플로우 전에는 항상 잔액을 먼저 확인할 것.

## BLOCKED 인벤토리 (2026-09-02 재집계)

⚠️ 종전 기재("케이스 기준 37건, 2026-08-06")는 **한 달 낡은 값**이었다. 그 사이
`03_Domestic [03]~[06]`(잔액 부족)·QR 송금·신분증 업로드가 해제됐고, `04_Menu_History`는
48케이스 완주로 BLOCKED가 확정됐다. 아래는 **`Old\*.yaml` 주석을 다시 읽어 재집계**한 것이다.

| 파일 | BLOCKED 케이스 | 원인 |
|---|---|---|
| `04_Menu_History_old` | `[02] [06] [09] [14] [17] [18] [22] [25]` (8) | 앱 버그 + 데이터 없음 (아래 묶음표) |
| `26_Menu_old` | `[18] [19] [23] [24]` (4) | 계좌 연결 penny test·OTP / Face ID·셀피 촬영 |
| `14_01_Deposit_seungsoo818_old` | `[02] [03] [04]` (3) | 수신 계좌가 WSB — **KJ 미배정 계정** |
| `18_Telecom_old` | `[06] [07]` (2) | 신청 완료 단계에 **외부 인증** — 2026-09-03 사유 변경 |
| `21_Card_old` | `[27] [28] [29]` (3) | **NFC 실물 카드 태깅** — 자동화 불가 |
| `21_Card_old` | `[24] [25]` (2) | Global QR 결제 — **실금전 위험으로 정책 제외** |
| `14_02_Deposit_test123_old` | `[06] [07]` (2) ⚠️ | 외국인 **이면서** GMEPay 미신청인 계정이 동시에 필요 |
| `11_IntlTopup_old` / `12_BillPayment_old` | KFTC 경로 각 1 | 은행계좌 출금 = **생체인증 + 실결제** |
| `25_Profile_old` | `[01-4]` 실제 번호 변경 (1) | OTP 인증 — 2026-08-18 사용자 확정 |
| `19_IssueCertificate_old` | 인증수단 선택 **이후** 전 구간 | WeCheck 본인인증(외부 WebView) |

⚠️ `14_02`는 주석에 `[06]~[10] 전체 BLOCKED`와 `[06]/[07]로 재구성`이 **둘 다 남아 있다**
(`[08]`은 코드가 이동됐다). 어느 쪽이 현행인지 **파일만으로는 확정되지 않는다** → ClickUp 대조 필요.

### 원인이 같은 것을 묶으면 종류는 훨씬 적다

| 원인 | 해당 케이스 | 성격 |
|---|---|---|
| 이력 다운로드 `fb_printHistory` — Android10에서 앱이 **미선언 WRITE 권한**을 체크 | `04` `[06] [09] [14] [18] [22]` → **5건 = 앱 버그 1개** | 개발팀 액션 |
| 외부 인증·하드웨어(생체·OTP·penny test·NFC·WeCheck) | `11` `12` `19` `21[27~29]` `25[01-4]` `26[18][19][23][24]` **`18[06][07]`** | QA가 못 푼다 |
| 데이터·계정 상태 부재 | `04 [02][17][18][25]` · `14_01 [02][03][04]` · `14_02 [06][07]` | **계정·데이터가 바뀌면 풀린다** |
| 실금전 위험(정책 제외) | `21 [24] [25]` Global QR | 사용자 판단 사항 |

### BLOCKED가 아닌 것 — 혼동 주의

- `04_Menu_History [36] [37] [38]` = **N/A(범위 밖)**. 쿠폰박스·리워드포인트는 내역(History) 탭에서
  **접근 자체가 안 되는 별도 메뉴**다. "막혔다"가 아니라 "이 파일 소관이 아니다".
- `23_MyWallet_old` = **N/A**. 구 UI에 화면이 없다(실행 케이스 0건, 문서용 파일).
- `25_Profile` 카메라 **촬영**은 "자동화 불가"가 아니라 **사용자 지시로 제외**한 것이다.
  2026-08-18에 촬영·갤러리 두 경로 모두 자동화 가능함이 확인됐다(종전 "불가" 기재는 오판).
- `04_Menu_History [02]`(재송금 결제수단 구분)는 앱 버그가 아니라 **상세 화면이 송금방법을
  "Bank Transfer"로만 표시**해 구분할 근거가 없는 것이다.
- **`18_Telecom [05]` = 2026-09-03 BLOCKED 해제 + 구현 완료**. 사유였던 "요금제 상품 미등록"이
  **상품 등록으로 해소**됐고, 이 케이스는 신청까지 가지 않고 **페이지 도달만** 확인하므로
  외부 인증과 무관하다 → 자동화했다. **검증 완료** — 1회차 전 구간 통과 / 2회차는 USB 끊김으로
  중단(코드 결함 아님) / **3회차 EXIT=0 · FAILED 0 · COMPLETED 99**. 파일은 73 → **82스텝**.
  → ClickUp `z8nfn6ma7k` **complete**, 체크리스트 361행 `Not Test` → **`Pass`** 반영 완료.
  ⚠️ 같은 "상품 미등록"이라도 **케이스마다 풀리는 범위가 다르다** — 일괄 해제도 일괄 유지도 틀린다.
- ⚠️ **상품 등록이 기존 통과 케이스 2개를 깨뜨렸다**(2026-09-03 실측, 같이 고쳤다):
  ① `[03]`의 `"Line SIM 요금제"` → 앱 라벨이 **`"후불 SIM 요금제 신청하기"`**로 바뀌었다(텍스트 전체매칭).
  ② `[04]`가 **`noDataAvailable`/"내역 없음"을 단언**하고 있었다 → 후불 목록에 상품이 생겨 실패한다.
     후불은 `packagelistRv`+`item_monthlypayment` 존재로 바꿨고, **선불은 아직 비어 있어** 그대로 뒀다.
  → 교훈: **데이터 부재를 단언해 둔 케이스는 데이터가 생기는 순간 깨진다.** BLOCKED 해제 시 같이 점검할 것.

⚠️ **계정 유형을 바꾸면 풀리는 BLOCKED가 있다.** 입금 바텀시트 클리핑은 화면 높이가 아니라
**시트 항목 수**(3개 이상이면 마지막 항목이 OS 내비바에 가림) 문제여서 계정에 따라 PASS/BLOCKED가 뒤집힌다.
BLOCKED로 적기 전에 다른 계정으로 한 번 더 확인할 것.

⚠️ `assertNotVisible`은 **오타·오역이 있어도 통과**한다. 실제로 `"GMEPay 가상 계좌"`(실제 라벨은
"GME **Pay** 가상 계좌")와 `"Apply Virtual Account"`(실제는 "가상계좌 신청") 두 건이 아무것도
검증하지 못한 채 통과하고 있었다. 같은 화면에서 `assertVisible`로 실제 라벨을 한 번은 확인할 것.


## 보류 중인 작업 / 남은 할 일 (2026-09-02 갱신)

| 항목 | 상태·조건 |
|---|---|
| **회귀 스위트 재작성** | **다른 세션이 진행 중**(2026-09-02). `Old\`를 그쪽이 편집하므로 **이 트랙 밖에서는 `Old\` yaml을 건드리지 않는다.** 사용자가 정한 3원칙(범위=전부 포함 / 실패 시 계속+자동복구 / 잔액은 시작 시 1회 확인 후 SKIP)은 `maestro-suite` skill에 있다 |
| **저장소 사본의 평문 자격정보** | `C:\GME\qa-automation` 커밋본은 **2026-09-01 스크럽 이전** 스냅샷이라 평문 PIN·비밀번호가 **266곳** 남아 있다(구 UI 사본 + 루트 V3 34개). 루트 V3는 애초에 스크럽 대상이 아니었다. **GitHub 푸시 전에 이력에서 제거**해야 한다(푸시 자체는 보류 중) |
| **저장소 재동기화 · 원본 정리 · CLAUDE.md 경로 갱신** | 스위트 재작성이 끝난 뒤. **원본 정리와 문서 갱신은 반드시 같이** — 문서만 바꾸면 다른 세션이 엉뚱한 데를 보고, 원본만 지우면 그 세션이 깨진다 |
| **ENVIRONMENT.md 공유 전 스크럽** | `25_Profile_old.yaml`의 **실 Gmail 주소 2건**을 env 변수로 뺀 뒤 공유. `Old\` 편집이라 스위트 작업 종료 후 |
| **stag 재검증 2건** | `19_IssueCertificate`(23곳) · `21_Card`(3곳) — 2026-08-28에 `livetest:id/` 하드코딩을 걷어냈으나 **수정 후 실행 검증 미완**. 기기 필요 |
| ~~**스크린샷 증적 정리**~~ | **2026-09-10 완료 — 정리 + 근본 해결까지.** 최상위 산출물 **191개(37MB)** 와 `Maestro\_archive\` 탐색 스크린샷 **106개(17MB)** 를 **삭제**했다(사용자 결정. 참조 0건, 실행하면 다시 생기는 산출물). 재발 방지로 **러너 2개가 실행 직후 `shots_runs\<시각>_<플로우>\` 로 회수**한다 — `Maestro\run_test.ps1`(스위트도 이 경로로 돈다)와 상위 `run_test.ps1`. ⚠️ 폴더명이 `shots_` 로 시작해야 `.gitignore(shots_*/)`·`sync_from_source.ps1` 이 함께 걸러낸다. 회수는 **실행 시각 이후 mtime** 기준이라 기존 파일을 건드리지 않고, 대괄호 이름(`[01]_....png`)에서도 동작함을 확인했다 |
| ~~**백업 파일 정리**~~ | **2026-09-10 완료.** 보류 조건이던 `run_suite.ps1` 재작성이 끝나 백업 5개(`run_suite.ps1.bak` 2개 · `run_test.ps1.bak` · `13_06`·`13_ReceiveOverseas` yaml 백업)와 임시 추출본 `Old\_tmp_hist*` 5개를 지웠다 |
| **ClickUp 상태 갱신** | 2026-08-19 리밋 대기분은 **8/20에 전부 처리 완료**. 지금 밀린 것은 **그 이후 확정분** — `03_Domestic` 6케이스 / `02_02` / `02_01` / `04_Menu_History` 48케이스 / `04_02 [13]` |
| 🐞 **청구서 내역 1개월 필터 오류** | ClickUp 미보고. **현재 재현되지 않는다**(청구서 데이터 0건이라 1개월도 정상 응답). 8/31에 데이터 6건 있을 때만 3회 재현 → **데이터가 있는 상태에서 재확인 후 보고** |
| **개편 UI(V3) 후속** | i18n B그룹 6개·C그룹 24개, A그룹 4개의 실기기 검증. **개편 빌드를 받을 수 없고 UI가 다시 바뀔 수 있어 급하지 않다**(2026-09-02 사용자 판단). 재개 시 새 APK로 ko→en 대응표부터 다시 뽑을 것 |
| **실제 결제 완주 자동화** | ~~보류~~ → **2026-08-28 진행 완료**. `11`·`12`·`20` 모두 결제 자체는 완주했고 남은 건 **결제 이후 홈 복귀 구간 검증**뿐이다. ⚠️ `11`·`12`는 취소 절차가 없어 **실행할 때마다 금액이 누적**된다 → 회귀 상시 포함 여부는 미결 |
